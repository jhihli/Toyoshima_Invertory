'use client';
import BoxesView from '@/app/ui/pallet/BoxesView';

/** Boxes on a pallet, reached from an MSFT order. Same view as the
 *  /sales-orders route — only the breadcrumb and back links differ. */
export default function Page() {
  return <BoxesView />;
}
